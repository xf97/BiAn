#!/usr/bin/python
#-*- coding: utf-8 -*-

'''
This program is used to get each dict in a json strcture.
'''

import os 
import sys
import json

'''

class getEachDict(self):
	def __init__(self, _json):
		self.json =  _json

	def run(self):
		queue = list()
		queue.append(self.json)
		result = list()
		while len(queue) > 0:
			data = queue.pop()
			for key in data:
				if type(data[key]) == dict 
'''
